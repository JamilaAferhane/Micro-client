import * as React from "react";
import Card from "@mui/material/Card";

export default function Footer() {
  return (
    <Card
      sx={{
        backgroundColor: "#000",
        display: "flex",
        marginTop: "auto",
        padding: 9,
      }}
    />
  );
}
